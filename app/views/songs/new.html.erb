<h1>Create a Song</h1>

<%= form_for @song  do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.text_field :artist_name %>
  <datalist id="song_genre_id">
    <% Genre.all.each do |genre| %>
    <option value="<%=genre.name%>">
      <%end%>
  <br />

  <%= f.submit %>
<% end %>
